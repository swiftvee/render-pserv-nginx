# nginx private server on Render

This is an example repo with a Dockerfile for running a Nginx reverse proxy as a **private service** on Render.

Use it as a template for your reverse proxy service.
